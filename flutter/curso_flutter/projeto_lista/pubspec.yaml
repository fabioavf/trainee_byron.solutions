name: mobix_cart_list_rx
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
    sdk: '>=2.7.0 <3.0.0'

dependencies:
    flutter:
        sdk: flutter

    cupertino_icons: ^0.1.3
    mobx: ^1.2.1
    flutter_mobx: ^1.1.0
    get_it:
    rxdart: ^0.24.1

dev_dependencies:
    build_runner: ^1.10.0
    mobx_codegen: ^1.1.0
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
